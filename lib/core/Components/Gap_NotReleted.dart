import 'package:Chat_app/core/Constant/screen_size.dart';
import 'package:flutter/material.dart';

class Gap_NotReleted extends StatelessWidget {
  const Gap_NotReleted({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: Gap_NotReleted_elements,
    );
  }
}
